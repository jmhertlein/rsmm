class AboutController < ApplicationController

  def index
    @josh_age = age Time.parse('1992-11-21')
  end

  def age(dob)
    now = Time.now.utc.to_date
    now.year - dob.year - ((now.month > dob.month || (now.month == dob.month && now.day >= dob.day)) ? 0 : 1)
  end
end
